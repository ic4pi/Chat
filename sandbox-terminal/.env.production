# When the sandbox-terminal is deployed to Vercel, it calls the agent API
# routes that live in the main chat app (chat-pearl-ten.vercel.app/api/...).
# The terminal routes everything through /api/* on that domain.
VITE_API_URL=https://chat-pearl-ten.vercel.app/api
